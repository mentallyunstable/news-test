targets:
  $default:
    builders:
      json_serializable:
        options:
          create_to_json: false
        generate_for:
          - "**/domain/**.dart"
          - "**/data/**.dart"
      retrofit_generator|retrofit_generator:
        generate_for:
          - "**/data_source/**_remote_data_source.dart"
